pipeline {
    agent { 
        node {
            label 'Node'
            }
    }

    environment {
        DOCKER_REGISTRY = "my-repo"
        DOCKERHUB = credentials('dockerhub')
        DOCKER_IMAGE_NAME = "my-python-web"
        DOCKER_IMAGE_TAG = "latest"
	SSH = credentials('ssh')
    }

    stages {
        stage('Build') {
            steps {
                echo "Building image.."
                sh '''
                docker build -t ${DOCKER_IMAGE_NAME}:${DOCKER_IMAGE_TAG} .
                '''
            }
        }
        
	stage('Run Ansible playbook') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'ssh', usernameVariable: 'SSH_USR', passwordVariable: 'SSH_PWS')]) {
                    sh "ansible-playbook playbook.yml --extra-vars 'ansible_ssh_user=${SSH_USR} ansible_ssh_pass=${SSH_PSW}'"
                }
            }
        }

	stage('Push Docker image') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'dockerhub', usernameVariable: 'DOCKERHUB_USR', passwordVariable: 'DOCKERHUB_PSW')]) {
                    sh "docker login -u ${DOCKERHUB_USR} -p ${DOCKERHUB_PSW}"
		    sh "docker tag ${DOCKER_IMAGE_NAME} ${DOCKERHUB_USR}/${DOCKER_REGISTRY}"
		    sh "docker push ${DOCKERHUB_USR}/${DOCKER_REGISTRY}"
                }
            }
        }
    }
}
